{% extends 'base.html.twig' %}

{% block title %}Ajouter une annonce{% endblock %}
{% block meta_description %}Ajout d'annonce sur le site, si tu vois ceci j'ai codé comme de la merde.{% endblock %}

{% block body %}
        <h1> Ajouter une annonce</h1>
        <div class="form-title">
            {{ form_start(form) }}
                <div class="form">
                    {{ form_label(form.title, "Titre") }}
                    {{ form_widget(form.title, { 'attr': {'class': 'form-all title-block'} }) }}
                </div>
                <div class="form">
                    {{ form_label(form.content, "Contenu") }}
                    {{ form_widget(form.content, { 'attr': {'class': 'form-all title-block'} }) }}
                </div>
                <div class="valider">
                    {{ form_label(form.Valider) }}
                    {{ form_widget(form.Valider, { 'attr': {'class': 'button-valider'} }) }}
                </div>
            {{ form_end(form) }}
        </div>
{% endblock %}