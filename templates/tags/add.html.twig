{% extends 'base.html.twig' %}

{% block title %}Ajouter une tag{% endblock %}
{% block meta_description %}Ajout de tag sur le site, si tu vois ceci j'ai codé comme de la merde.{% endblock %}

{% block body %}
        <h1> Ajouter une tag</h1>
        <div class="form-title">
            {{ form_start(form) }}
                <div class="form">
                    {{ form_label(form.name, "Titre") }}
                    {{ form_widget(form.name, { 'attr': {'class': ' form-all title-block'} }) }}
                </div>
                <div class="valider">
                    {{ form_label(form.Valider) }}
                    {{ form_widget(form.Valider, { 'attr': {'class': 'button-valider'} }) }}
                </div>
            {{ form_end(form) }}
        </div>
{% endblock %}